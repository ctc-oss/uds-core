stages:
  - build

default:
  image: nexus.draco.lab:8083/uds-builder:latest
  tags:
    - docker

build:
  stage: build
  script: |
    uds run create-standard-package
#   rules:
#     - if: '$CI_COMMIT_TAG'
#       when: always
#     # Rule 1: Run automatically on the default branch (e.g., 'main') for push events.
#     - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH && $CI_PIPELINE_SOURCE == "push"'
#       when: manual
#     - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
#       when: never
#     # Rule 2: On any other branch, the job is available but must be started manually.
#     - if: '$CI_COMMIT_BRANCH != $CI_DEFAULT_BRANCH'
#       when: manual
#     # Rule 3: Allow manual runs initiated via the "Run pipeline" button in the UI.
#     - if: '$CI_PIPELINE_SOURCE == "web"'
#       when: manual
